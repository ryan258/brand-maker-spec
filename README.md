# Brand System Maker

A stateless FastAPI service that turns one parody brand name into a complete,
validated brand kit using OpenRouter. See `spec.md` for the product contract.

Implementation and operating instructions are added as each verified slice lands.
